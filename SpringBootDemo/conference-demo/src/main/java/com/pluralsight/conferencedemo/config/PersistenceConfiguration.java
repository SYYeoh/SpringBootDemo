package com.pluralsight.conferencedemo.config;
/*
import javax.sql.DataSource;
import lombok.extern.slf4j.Slf4j;
import org.springframework.boot.jdbc.DataSourceBuilder;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
@Slf4j
public class PersistenceConfiguration {

  @Bean
  public DataSource dataSource() {
    DataSourceBuilder<?> builder = DataSourceBuilder.create();
    builder.url("jdbc:postgresql://localhost:5432/conference_app?currentSchema=conference_app");
    builder.username("${LOCAL_CONFERENCE_USERNAME}");
    builder.password("${LOCAL_CONFERENCE_PASSWORD}");
    log.debug("My custom datasource bean has been initialized and set");
    return builder.build();
  }

}
*/
